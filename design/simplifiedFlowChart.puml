@startuml
start

:Initialize simulation parameters;
:Create output directory;

while (More replications?) is (yes)
  :Set random seed;
  :Generate bug arrivals;
  :Initialize developer and QA availability;

  while (More bug arrivals?) is (yes)
    :Assign to developer (wait if busy);
    :Developer fixes bug;
    :Assign to QA (wait if busy);
    :QA tests bug;
    if (QA returns for rework?) then (yes)
      :Send bug back to developer;
      -[back]-> while (More bug arrivals?) is (yes)
    else (no)
      :Bug closed;
    endif
  endwhile

  :Record metrics for replication;
endwhile

:Aggregate results;
:Plot and save outputs;
:Write summary report;

stop
@enduml